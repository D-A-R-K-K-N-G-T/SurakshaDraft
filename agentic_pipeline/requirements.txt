pydantic>=2
pydantic-settings>=2
langgraph
langchain-core
langchain-google-genai
langchain-ibm
python-dotenv
pillow

# --- HTTP service (agentic_pipeline/service.py) ---
# These were previously assumed to be present in the ambient environment; the
# container image has nothing but this file, so they are pinned here.
fastapi
uvicorn[standard]
# Only imported when auth_mode == "firebase" (the config default). If a
# deployment runs auth_mode=disabled|demo this can be dropped — it is the
# single heaviest dependency in the image.
firebase-admin

# --- persistence (Phase 1: database foundation) ---
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary,pool]>=3.1
boto3>=1.34
cryptography>=41.0.0
celery>=5.3
redis>=5.0

# --- tests ---
pytest>=8
# Ephemeral Postgres for the migration round-trip / repository tests. Optional:
# the DB-backed tests skip cleanly when no server (and this plugin) is present.
pytest-postgresql>=6